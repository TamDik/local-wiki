import {WikiNSManager, parseWikiLocation} from './model/wiki_location';
import {Wiki} from './model/wiki';

(() => {
    const wiki: Wiki = new Wiki();
    const wikiNS: string = 'Wiki';
    const wikiName: string = 'index';
})();
